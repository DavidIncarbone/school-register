// import type { StateCreator } from "zustand";

// export type AuthSlice = {
//     isAuth: boolean;
//     setIsAuth: (bool: boolean) => void;
// };

// export const createAuthSlice: StateCreator<AuthSlice, [], [], AuthSlice> = (
//     set
// ) => ({
//     isAuth: false,
//     setIsAuth: function (bool) {
//         set(() => ({ isAuth: bool }));
//     },
// });
